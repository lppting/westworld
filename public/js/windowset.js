$(function(){
             var s = true;
   //          $('#mySwitch input').bootstrapSwitch('state',s);
/*              if(s === false){
                    $(".ctrl").addClass("selected").siblings().removeClass("selected");
                 }
                 if(s === true){
                    $(".ctrl").removeClass("selected");
                 }
             $('#mySwitch').on('switchChange.bootstrapSwitch', function (e, data) {
                 if(s === false){
                    s = data;
                    $(".ctrl").addClass("selected").siblings().removeClass("selected");
                 }
                 if(s === true){
                    s = data;
                    $(".ctrl").removeClass("selected");
                 }
                 console.log(data);
            });        */
            
})